# Update the Go configuration module and CLI default paths to transition from the monolithic 'workflows/' directory to the new sibling directories 'flows/' and 'subflows/', rename the existing directory on disk, and create the subflows directory to support modular flow storage.

This task focuses on transitioning the workspace from a monolithic workflows structure to a modular flows layout. It requires updating default file paths and config parsing inside cmd/build/main.go and internal/config/ to support the sibling directories 'flows/' and 'subflows/' instead of the legacy 'workflows/' directory. Specifically, the hardcoded path references inside 'deploy-flow' and 'sync-flows' subcommands in main.go must be updated, the legacy 'workflows/' directory renamed to 'flows/', and the new 'subflows/' directory created. This prepares the directory environment for subsequent modular flow partitioning, deployment, and bidirectional sync state-tracking.
