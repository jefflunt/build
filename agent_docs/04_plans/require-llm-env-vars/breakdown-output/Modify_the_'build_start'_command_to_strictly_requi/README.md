# Modify the 'build start' command to strictly require the BUILD_LLM_PROVIDER and BUILD_LLM_MODEL environment variables. If missing, the program should halt immediately, query 'opencode models' for a list of valid options, and print a detailed setup guide before exiting with a status code of 1. Documentation must also be updated to reflect these explicit configuration requirements.

The orchestrator's 'build start' command currently falls back to a hardcoded Google Gemini model, which can lead to silent failures or unintended model usage. This task implements strict enforcement of the 'BUILD_LLM_PROVIDER' and 'BUILD_LLM_MODEL' environment variables. If either variable is missing, the application will halt execution, attempt to retrieve available models by executing 'opencode models' (with a static fallback if it fails), and display a comprehensive setup guide before exiting with code 1. Additionally, the project documentation must be updated to clearly communicate these mandatory configuration steps to the user.
