# Update the configuration module in `internal/config/config.go` to strictly require and validate the `node_red_url` field in `~/.build/config.yml`, and update all relevant config parser unit tests in `internal/config/config_test.go` to keep the build and tests passing.

This task involves modifying the configuration system in `internal/config/config.go` to support a new mandatory parameter, `node_red_url`. Currently, the configuration parsing mechanism only looks for the `agent_adapter` parameter. The `Config` struct needs to be extended with a `NodeRedURL` field, and the parser inside `Parse` must be updated to read `node_red_url` from the input stream. If the field is missing or empty, a clear and actionable error message must be returned to the caller.

Because this field is now strictly required, all existing unit tests in `internal/config/config_test.go` and any files loading config (such as templates or CLI commands) will fail to parse unless they are updated with a valid `node_red_url` configuration. Therefore, this task also entails updating all mocked configurations, YAML payloads, and temp configs inside test files to supply a valid URL (such as `http://localhost:1880`). This ensures the module is fully self-contained, compliant, and maintains the test suite in a passing state without disrupting other components.
